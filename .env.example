# Copy this file to .env and fill in real credentials

# ── Server ──
PORT=3000
NODE_ENV=development

# ── MongoDB ──
# Development: mongodb://localhost:27017/notification_service
# Production:  mongodb+srv://user:pass@cluster.mongodb.net/notification_service (Atlas / external)
MONGODB_URI=mongodb://localhost:27017/notification_service

# ── Redis (BullMQ) ──
# Redis runs as a Docker container in both dev and prod.
# Supports REDIS_URL (takes priority) or individual REDIS_HOST/REDIS_PORT/REDIS_PASSWORD
# Development: redis://localhost:6379
# Production:  docker-compose.prod.yml overrides this to redis://redis:6379
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# ── Admin Auth ──
# Used for admin panel JWT tokens — use a long random string in production
ADMIN_JWT_SECRET=change_me_to_a_random_64_char_string

# ── Credential Encryption ──
# 32 bytes hex (64 hex chars) — used to AES-256-GCM encrypt stored SMTP/Twilio/FCM credentials
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CREDENTIAL_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ── Admin Seed User ──
ADMIN_DEFAULT_EMAIL=admin@notification.local
ADMIN_DEFAULT_PASSWORD=change-this-password

# ── Rate Limiting ──
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# ── CORS ──
FRONTEND_URL=http://localhost:3001
